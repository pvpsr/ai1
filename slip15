Q.1)Write a Program to Implement Monkey Banana Problem using Python


def monkey_banana():
    state = {
        "monkey": "door",
        "box": "window",
        "on_box": False,
        "banana": "middle",
        "has_banana": False
    }

    print("Initial State:", state)

    # Step 1: Monkey walks to box
    state["monkey"] = state["box"]
    print("\nMonkey moves to the box:", state)

    # Step 2: Monkey pushes box to banana
    state["box"] = state["banana"]
    state["monkey"] = state["banana"]
    print("\nMonkey pushes box under the banana:", state)

    # Step 3: Monkey climbs on the box
    state["on_box"] = True
    print("\nMonkey climbs on the box:", state)

    # Step 4: Monkey takes the banana
    state["has_banana"] = True
    print("\nMonkey picks the banana:", state)

    print("\nFinal State:", state)


monkey_banana()



Q.2) Write a program to implement Iterative Deepening DFS algorithm.  
[ Goal Node =G]


# Tree as adjacency list
graph = {
    'A': ['B', 'C'],
    'B': ['D', 'E'],
    'C': ['F', 'G'],
    'D': ['H', 'I'],
    'E': [],
    'F': ['K'],
    'G': [],
    'H': [],
    'I': [],
    'K': []
}

# Depth Limited Search
def DLS(node, goal, limit):
    print(f"Visiting: {node}")
    if node == goal:
        return True
    if limit <= 0:
        return False
    
    for child in graph[node]:
        if DLS(child, goal, limit - 1):
            return True
    return False

# Iterative Deepening DFS
def IDDFS(start, goal, max_depth):
    for depth in range(max_depth + 1):
        print(f"\n--- Depth Limit = {depth} ---")
        if DLS(start, goal, depth):
            print(f"\nGoal '{goal}' found at depth {depth}")
            return
    print("Goal not found")

# Run
IDDFS('A', 'G', 10)



